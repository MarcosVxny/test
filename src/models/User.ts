//Para fazer a conexão da página de Cadastrar Usuário com o back-end
interface User {
    id: number;
    nome: string;
    usuario: string;
    senha: string;
    foto: string;
}

export default User;
